# ===== Microsoft Foundry 설정 =====
# Microsoft Foundry 프로젝트 엔드포인트
# 형식: https://<resource-name>.services.ai.azure.com/api/projects/<project-name>
PROJECT_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project

# 배포된 채팅 모델 이름 (예: gpt-4o, gpt-4.1, gpt-5.4)
# Foundry IQ 자동 지식 베이스 생성에서는 배포 이름을 실제 모델 이름과 같게 사용하세요.
MODEL_DEPLOYMENT_NAME=gpt-5.4

# ===== 예제 06 (RAG) — Azure AI Search 연동 =====
# Azure AI Search 엔드포인트
SEARCH_SERVICE_ENDPOINT=https://your-search-service.search.windows.net

# RAG 인덱스 이름 (없으면 자동 생성)
SEARCH_INDEX_NAME=maf-lab-knowledge-v2

# 문서 임베딩 호출 + Foundry IQ 쿼리 벡터라이저용 Azure OpenAI 엔드포인트
# 형식: https://<resource-name>.cognitiveservices.azure.com/
AZURE_OPENAI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# 임베딩 모델 배포 이름 (예: text-embedding-3-large)
EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-large

# 임베딩 배포의 실제 모델 이름 (Foundry IQ 쿼리 벡터라이저 설정용)
EMBEDDING_MODEL_NAME=text-embedding-3-large

# Azure OpenAI API 버전
AZURE_OPENAI_API_VERSION=2024-10-21

# ===== 예제 06 변형 (Foundry IQ RAG) — 지식 베이스 + agentic retrieval =====
# 기존 하이브리드 예제와 충돌하지 않도록 별도 인덱스 이름을 사용합니다.
# (없으면 src/06_rag_agent_foundry_iq.py 첫 실행 시 자동 생성·시드)
SEARCH_INDEX_NAME_IQ=maf-lab-knowledge-iq-v3

# (선택) agentic 지식 베이스용 모델 공급자 리소스 루트 URL.
# /api/projects/...가 붙은 Foundry 프로젝트 엔드포인트가 아니며, 미설정 시 AZURE_OPENAI_ENDPOINT 사용.
# 지원 도메인: openai.azure.com, services.ai.azure.com, cognitiveservices.azure.com
# AZURE_OPENAI_RESOURCE_URL=https://your-resource.openai.azure.com/

# (선택) 질의 계획 추론 강도 (minimal/low/medium, 기본값: low)
# minimal은 LLM 질의 계획을 건너뛰고 단일 검색 의도를 그대로 실행합니다.
# FOUNDRY_IQ_REASONING_EFFORT=low
